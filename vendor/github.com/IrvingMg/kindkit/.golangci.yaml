version: "2"

linters:
  default: standard # errcheck, govet, ineffassign, staticcheck, unused

formatters:
  enable:
    - gci

settings:
  gci:
    sections:
      - standard
      - default
      - prefix(github.com/IrvingMg/kindkit)
