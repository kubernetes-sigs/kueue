apiVersion: v1
kind: ConfigMap
metadata:
  name: kueue-populator-config
  namespace: kueue-system
data:
  controller_manager_config.yaml: |
    localQueueName: default
    # managedJobsNamespaceSelector:
    #   matchLabels:
    #     key: value
