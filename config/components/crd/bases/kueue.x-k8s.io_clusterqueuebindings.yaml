---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.5
  name: clusterqueuebindings.kueue.x-k8s.io
spec:
  group: kueue.x-k8s.io
  names:
    kind: ClusterQueueBinding
    listKind: ClusterQueueBindingList
    plural: clusterqueuebindings
    singular: clusterqueuebinding
  scope: Namespaced
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: ClusterQueueBinding is the Schema for the clusterqueuebindings
          API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              clusterQueue:
                description: clusterQueue is a reference to a clusterQueue that backs
                  this localQueue.
                maxLength: 253
                pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                type: string
              subjects:
                description: subjects is a list of references to the objects that
                  are allowed to access the localQueue.
                items:
                  properties:
                    kind:
                      enum:
                      - Group
                      - User
                      - ServiceAccount
                      type: string
                    name:
                      type: string
                    namespace:
                      type: string
                  type: object
                type: array
            type: object
          status:
            description: ClusterQueueBindingStatus defines the observed state of ClusterQueueBinding
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
