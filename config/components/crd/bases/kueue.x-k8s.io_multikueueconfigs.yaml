---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.12.0
  name: multikueueconfigs.kueue.x-k8s.io
spec:
  group: kueue.x-k8s.io
  names:
    kind: MultiKueueConfig
    listKind: MultiKueueConfigList
    plural: multikueueconfigs
    singular: multikueueconfig
  scope: Cluster
  versions:
  - name: v1beta1
    schema:
      openAPIV3Schema:
        description: MultiKueueConfig is the Schema for the provisioningrequestconfig
          API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MultiKueueConfigSpec defines the desired state of MultiKueueConfig
            properties:
              clusters:
                description: clusters contains the list of configurations for using
                  worker clusters.
                items:
                  properties:
                    kubeconfigRef:
                      properties:
                        configKey:
                          type: string
                        secretName:
                          type: string
                        secretNamespace:
                          type: string
                      required:
                      - configKey
                      - secretName
                      - secretNamespace
                      type: object
                    name:
                      type: string
                  required:
                  - kubeconfigRef
                  - name
                  type: object
                maxItems: 100
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
            required:
            - clusters
            type: object
        type: object
    served: true
    storage: true
