---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.21.0
  name: capacityproviders.kueue.x-k8s.io
spec:
  group: kueue.x-k8s.io
  names:
    kind: CapacityProvider
    listKind: CapacityProviderList
    plural: capacityproviders
    shortNames:
    - cp
    singular: capacityprovider
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: CapacityProvider is the Schema for the capacityproviders API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            properties:
              controllerName:
                allOf:
                - maxLength: 253
                  minLength: 1
                - maxLength: 253
                  minLength: 1
                description: |-
                  controllerName identifies the controller publishing capacity.
                  This field is immutable.
                type: string
                x-kubernetes-validations:
                - message: field is immutable
                  rule: self == oldSelf
              orchestratedFlavors:
                description: |-
                  orchestratedFlavors identifies the ResourceFlavors for which this provider may
                  publish capacity. DQO ignores entries in status.capacity.flavors whose
                  names are not listed here.
                items:
                  description: |-
                    CapacityProviderOrchestratedFlavor identifies a flavor managed by a provider.
                    The container allows the mapping to be extended in a future API version.
                  properties:
                    name:
                      description: name identifies the ResourceFlavor managed by this
                        provider.
                      maxLength: 253
                      pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                      type: string
                  required:
                  - name
                  type: object
                maxItems: 64
                minItems: 1
                type: array
                x-kubernetes-list-map-keys:
                - name
                x-kubernetes-list-type: map
              parameters:
                description: |-
                  parameters optionally references implementation-specific configuration.
                  DQO does not read or validate the referenced object.
                properties:
                  apiGroup:
                    description: apiGroup is the group for the resource being referenced.
                    maxLength: 253
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                    type: string
                  kind:
                    description: kind is the type of the resource being referenced.
                    maxLength: 63
                    minLength: 1
                    pattern: ^(?i)[a-z]([-a-z0-9]*[a-z0-9])?$
                    type: string
                  name:
                    description: name is the name of the resource being referenced.
                    maxLength: 63
                    minLength: 1
                    pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                    type: string
                required:
                - apiGroup
                - kind
                - name
                type: object
            required:
            - controllerName
            - orchestratedFlavors
            type: object
          status:
            properties:
              capacity:
                description: capacity is the normalized capacity published by the
                  provider.
                properties:
                  flavors:
                    description: flavors contains capacity per flavor and resource.
                    items:
                      properties:
                        name:
                          description: name identifies the ResourceFlavor whose capacity
                            is reported.
                          maxLength: 253
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*$
                          type: string
                        resources:
                          additionalProperties:
                            anyOf:
                            - type: integer
                            - type: string
                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                            x-kubernetes-int-or-string: true
                          description: resources contains total capacity by resource
                            name.
                          type: object
                          x-kubernetes-validations:
                          - message: resource capacity must have between 1 and 64
                              entries
                            rule: size(self) >= 1 && size(self) <= 64
                      required:
                      - name
                      - resources
                      type: object
                    maxItems: 64
                    type: array
                    x-kubernetes-list-map-keys:
                    - name
                    x-kubernetes-list-type: map
                required:
                - flavors
                type: object
              conditions:
                description: conditions represents the current state of this provider.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                maxItems: 16
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
