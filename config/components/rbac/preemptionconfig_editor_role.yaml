# permissions for end users to edit preemptionconfigs.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: preemptionconfig-editor-role
  labels:
    rbac.kueue.x-k8s.io/role: "preemptionconfig-editor"
    rbac.kueue.x-k8s.io/batch-admin: "true"
rules:
- apiGroups:
  - kueue.x-k8s.io
  resources:
  - preemptionconfigs
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
