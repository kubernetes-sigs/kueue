# AllAlpha=true settings for Kueue.

# Use default settings as a base.
resources:
- ../default
- topology_editor_role.yaml
- topology_viewer_role.yaml

patches:
# Modify feature gates to enable AllAlpha=true
- path: manager_config_patch.yaml
