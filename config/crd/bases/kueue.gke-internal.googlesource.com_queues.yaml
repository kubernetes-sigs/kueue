
---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.7.0
  creationTimestamp: null
  name: queues.kueue.gke-internal.googlesource.com
spec:
  group: kueue.gke-internal.googlesource.com
  names:
    kind: Queue
    listKind: QueueList
    plural: queues
    singular: queue
  scope: Cluster
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Queue is the Schema for the queues API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: QueueSpec defines the desired state of Queue
            properties:
              capacities:
                description: capacities are an ordered list of names that refer to
                  QueueCapacity objects from which resources are allocated to this
                  Queue. Workloads assigned to this Queue are assigned to the first
                  QueueCapacity in this list that has available resources.
                items:
                  description: CapacityReference is the name of the QueueCapacity.
                  type: string
                type: array
                x-kubernetes-list-type: set
              priority:
                description: priority of the queue. When multiple Queues reference
                  the same QueueCapacity, workloads in Queues with higher priority
                  consume the shared QueueCapacity first.
                format: int64
                type: integer
            required:
            - priority
            type: object
          status:
            description: QueueStatus defines the observed state of Queue
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
status:
  acceptedNames:
    kind: ""
    plural: ""
  conditions: []
  storedVersions: []
