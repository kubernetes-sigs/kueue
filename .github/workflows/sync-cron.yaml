name: 'KDEV: Add with cron'
run-name: 'KDEV: Add scheduled run #${{github.run_number}}'
on:
  # workflow_dispatch: {}
  schedule:
    - cron:  '8 * * * *'
jobs:
  trasc:
    uses: epam/kubernetes-kueue/.github/workflows/add-issues-by-user.yaml@main
    with:
      gh_repo: kubernetes-sigs/kueue
      gh_user: trasc
      status: open
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
  akranga:
    uses: epam/kubernetes-kueue/.github/workflows/add-issues-by-user.yaml@main
    with:
      gh_repo: kubernetes-sigs/kueue
      gh_user: akranga
      status: open      
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
  oginskis:
    uses: epam/kubernetes-kueue/.github/workflows/add-issues-by-user.yaml@main
    with:
      gh_repo: kubernetes-sigs/kueue
      gh_user: oginskis
      status: open      
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
  stuton:
    uses: epam/kubernetes-kueue/.github/workflows/add-issues-by-user.yaml@main
    with:
      gh_repo: kubernetes-sigs/kueue
      gh_user: stuton
      status: open
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
  achernevskii:
    uses: epam/kubernetes-kueue/.github/workflows/add-issues-by-user.yaml@main
    with:
      gh_repo: kubernetes-sigs/kueue
      gh_user: achernevskii
      status: open
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
  mbobrovskyi:
    uses: epam/kubernetes-kueue/.github/workflows/add-issues-by-user.yaml@main
    with:
      gh_repo: kubernetes-sigs/kueue
      gh_user: mbobrovskyi
      status: open
    secrets:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}
